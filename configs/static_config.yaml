components_manager:
    task_processors:                  # Task processor is an executor for coroutine tasks

        main-task-processor:          # Make a task processor for CPU-bound couroutine tasks.
            worker_threads: $worker-threads         # Process tasks in 4 threads.

        fs-task-processor:            # Make a separate task processor for filesystem bound tasks.
            worker_threads: $worker-fs-threads

    default_task_processor: main-task-processor

    components:                       # Configuring components that were registered via component_list
        server:
            listener:                 # configuring the main listening socket...
                port: $server-port            # ...to listen on this port and...
                task_processor: main-task-processor    # ...process incoming requests on this task processor.
        logging:
            fs-task-processor: fs-task-processor
            loggers:
                default:
                    file_path: '@stderr'
                    level: $logger-level
                    overflow_behavior: discard  # Drop logs if the system is too busy to write them down.

        # Dynamic config options. Cache is disabled, updates are disabled.
        dynamic-config:
            # For most of userver dynamic configs, defaults are used, some are overridden here.
            # See userver "dynamic config" docs for what configs exist.
            defaults:
                HTTP_CLIENT_CONNECTION_POOL_SIZE: 1000
                POSTGRES_DEFAULT_COMMAND_CONTROL:
                    network_timeout_ms: 750
                    statement_timeout_ms: 500

        testsuite-support: {}

        http-client:
            load-enabled: $is-testing
            fs-task-processor: fs-task-processor

        tests-control:
            load-enabled: $is-testing
            path: /tests/{action}
            method: POST
            task_processor: main-task-processor
            
        handler-ping:
            path: /ping
            method: GET
            task_processor: main-task-processor
            throttling_enabled: false
            url_trailing_slash: strict-match

        pg-tms-db:
            dbconnection: $dbconnection
            dbconnection#env: DB_CONNECTION
            blocking_task_processor: fs-task-processor
            dns_resolver: async
            sync-start: false
            connlimit_mode: manual

        handler-user-register-post:
            path: /v1/auth/register
            method: POST
            task_processor: main-task-processor
            throttling_enabled: false
            url_trailing_slash: strict-match

        handler-user-login-post:
            path: /v1/auth/login
            method: POST
            task_processor: main-task-processor
            throttling_enabled: false
            url_trailing_slash: strict-match

        handler-user-task-post:
            path: /v1/tasks/
            method: POST
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes: 
                    - admin 
                    - user

        handler-user-task-list-get:
            path: /v1/tasks/
            method: GET
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin
                    - user

        handler-user-task-get:
            path: /v1/tasks/{task_id}
            method: GET
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin
                    - user

        handler-user-task-put:
            path: /v1/tasks/{task_id}
            method: PUT
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin
                    - user

        handler-user-task-delete:
            path: /v1/tasks/{task_id}
            method: DELETE
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin
                    - user

        handler-admin-user-post:
            path: /v1/admin/users/
            method: POST
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin

        handler-admin-user-put:
            path: /v1/admin/users/{user_id}
            method: PUT
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin

        handler-admin-user-get:
            path: /v1/admin/users/{user_id}
            method: GET
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin

        handler-admin-user-delete:
            path: /v1/admin/users/{user_id}
            method: DELETE
            task_processor: main-task-processor
            auth:
                types:
                    - bearer
                scopes:
                    - admin

        # handler-admin-task-post:
        #     path: /v1/tasks/
        #     method: POST
        #     task_processor: main-task-processor
        #     auth:
        #         types:
        #             - bearer
        #         scopes:
        #             - admin
        
        # handler-admin-task-put:
        #     path: /v1/admin/tasks/{task_id}
        #     method: PUT
        #     task_processor: main-task-processor
        #     auth:
        #         types:
        #             - bearer
        #         scopes:
        #             - admin

        # handler-admin-task-get:
        #     path: /v1/admin/tasks/{task_id}
        #     method: GET
        #     task_processor: main-task-processor
        #     auth:
        #         types:
        #             - bearer
        #         scopes:
        #             - admin

        # handler-admin-task-list-get:
        #     path: /v1/admin/tasks
        #     method: GET
        #     task_processor: main-task-processor
        #     auth:
        #         types:
        #             - bearer
        #         scopes:
        #             - admin

        # handler-admin-task-delete:
        #     path: /v1/admin/tasks/{task_id}
        #     method: DELETE
        #     task_processor: main-task-processor
        #     auth:
        #         types:
        #             - bearer
        #         scopes:
        #             - admin

        handler-extra-echo-get:
            path: /echo
            method: GET
            task_processor: main-task-processor

        dns-client:
            fs-task-processor: fs-task-processor
